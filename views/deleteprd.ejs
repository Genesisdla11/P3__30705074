<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Eliminar Producto</title>

    <header>
      <nav>
        <h1>Eliminar Producto</h1>
          <a href="/home">Home</a>
          <a href="/productos">Volver</a>
      </nav>
  </header>
    
    <style>
    body {
      font-family: sans-serif;
      background-color: #ffffff;
      margin: 0;
      padding: 0;
    }

    h3{
      margin: 45px;
      text-align: center;
    }
    
    form {
      width: 500px;
      margin: 2.5em auto;
      background-color:  #f2f2f2;
      padding: 20px;
      border-radius: 10px;
    }

    header nav a:hover{
      background-color: black;
      color: white;
    }
    
    button {
      background-color: #181e1f;
      color: #ffffff;
      font-size: 18px;
      padding: 10px;
      border-radius: 5px;
      display: block;
      padding: 0.5rem 1rem;
      width: 200px;
      margin: 0 auto;
    }
    
    header{
      background-color: #f2f2f2;
      text-align: center;
      padding: 25px 0;
    }

    nav a{
      display: inline-block;
      text-decoration: none;
      color: #696969;
      margin: 0 40px;
      padding: 5px 15px;
      border-radius: 30px;
    }
</style>    

</head>
<body>
    <div>
      <h3>¿Está seguro de eliminar este producto?</h3>
        <form action="/deleteprd/<%= productos.id %>" method="get">
            <h4><%= productos.nombre %></h4>
            <h4><%= productos.codigo %></h4>
            <h4><%= productos.precio %></h4>
            <h4><%= productos.marca %></h4>
            <h4><%= productos.material %></h4>
            <h4><%= productos.descripcion %></h4>
            <button type="submit" class="btn btn-primary">Eliminar Producto</button>
        </form>
    </div>
</body>
</html>