<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/stylesheets/admin.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
  <title>Categoria</title>

  <style>
    table {
      border-collapse: collapse;
      width: 100%;
    }

    .botones {
      text-align: center;
    }

    table {
      border-collapse: collapse;
      width: 100%;
    }

    thead {
      background-color: #eee;
      text-align: center;
      padding: 15px;
    }

    th, td {
      border: 1px solid #ccc;
      padding: 15px;
    }

    th {
      background-color: #eee;
    }

    .botones {
      text-align: center;
    }

    .input-group{
      padding: 10px;
      width:50%; 
      left: 80px;;
      text-align:center;
      margin:auto;
    }

    tbody{
      background-color: #ffffff;
    }
    
  </style>
<body>
    <header>
        <nav>
            <h3>Categorias </h3><br> 
            <a href="/home">Home</a>
            <a href="/categorias">Categorias</a>
            <a href="/productos">Productos</a>
            <a href="/imagenes">Imagenes</a>
        </nav>
</head><br>

<body>
  <table>
    <thead>
      <tr>
        <th>N.</th>
        <th>Nombre</th>
        <td><a href="/ctgagg" class="btn">Agregar</a></td>
      </tr>
    </thead>
    <tbody>
        <tr>
            <% if (categorias) { %>
                <% for (let i = 0; i < categorias.length; i++){  %>
                    <% if (categorias) { %>
                        <tr>
                            <td><%= (i + 1) %></td>
                            <td><%= categorias[i].nombre %></td>
                            <td>
                                <div class="input-group">
                                    <a href="/ctgdelete/<%= categorias[i].id%>" class="btn btn-outline-primary">Eliminar</a>
                                    <a href="/ctgedit/<%= categorias[i].id%>" class="btn btn-outline-success">Actualizar</a>

                                </div>
                            </td>
                        </tr>    
                    <% } %>
                <% } %>
            <% } else { %>
                    <tr>
                        <th>No hay categorias en el sistema</th>
                    </tr>
                <% }   %>
        </tr>
      </tr>
    </tbody>
  </table>
</body>
</html>
