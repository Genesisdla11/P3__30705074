<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Agregar Producto</title>

    <style>
        body {
      font-family: sans-serif;
      background-color: #ffffff;
      margin: 200px;
      padding: 100px;
    }
    
    form {
      width: 500px;
      margin: 0 auto;
      background-color: #ffffff;
      padding: 20px;
      border-radius: 10px;
    }
    
    label {
      display: block;
      margin-bottom: 10px;
      font-size: 18px;
      font-weight: 600;
    }
    
    input, textarea {
      width: 100%;
      padding: 10px;
      border: 1px solid #ccc;
      margin-bottom: 10px;
    }
    
    button {
      background-color: #181e1f;
      color: #ffffff;
      font-size: 18px;
      padding: 10px;
      border-radius: 5px;
      
    }
    
    button:hover {
      background-color: rgb(42, 179, 221) 33;
    }
    
    
        </style>
</head>
<body>
    <div>
        <form action="/aggprd" method="post">
            <input type="text" name="nombre" id="nombre" placeholder="Ingrese el nombre del producto" required maxlength="200" minlength="3">
            <input type="number" name="codigo" id="codigo" placeholder="Ingrese el codigo del producto" required>
            <input type="number" name="precio" id="precio" placeholder="Ingrese el precio del producto" required>
            <input type="text" name="marca" id="marca" placeholder="Ingrese la marca del producto" required maxlength="25" minlength="4">
            <input type="text" name="material" id="material" placeholder="Ingrese el material del producto" required>
            <select name="categoria" id="categoria">
                <option value="" >--Seleccione--</option>
                <% if (categorias) { %>
                    <% for (let i = 0; i < categorias.length; i++){  %>
                        <% if (categorias) { %>
                            <option value="<%= categorias[i].id %>" id="categoria_id" name="categoria_id"><%= categorias[i].nombre %></option>
    
                        <% } %>
                    <% } %>
                <% } else { %>
                            <option value="">No existen categorias en el sistema</option>
                    <% }   %>
            </select>
            <input type="textarea" name="descripcion" id="descripcion" placeholder="Ingrese la descripcion del producto" maxlength="600" minlength="10">
            <button type="submit" class="btn btn-primary">Agregar Producto</button>
        </form>
    </div>
</body>
</html>
