<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Producto</title>

    <header>
      <nav>
        <h1>Editar Producto</h1>
          <a href="/home">Home</a>
          <a href="/productos">Volver</a>
      </nav>
  </header>
    
    <style>
      body {
        font-family: sans-serif;
        background-color: #ffffff;
        margin:  0;
        padding: 0;
      }
    
      form {
        width: 500px;
        margin: 2.5em auto;
        background-color: #f2f2f2 ;
        padding: 40px;
        border-radius: 10px;
      }
  
      input, textarea {
        width: 94%;
        padding: 15px;
        border: 1px solid #ccc;
        margin-bottom: 10px;
      }
    
    
      button {
        background-color: #181e1f;
        color: #ffffff;
        font-size: 18px;
        border-radius: 5px;
        display: block;
        padding: 0.5rem 1rem;
        width: 200px;
        margin: 0 auto; 
      }

      header nav a:hover{
        background-color: black;
        color: white;
      }
    
      header{
        background-color: #f2f2f2;
        text-align: center;
        padding: 25px 0;
      }

      nav a{
        display: inline-block;
        text-decoration: none;
        color: #696969;
        margin: 0 40px;
        padding: 5px 15px;
        border-radius: 30px;
      }
    </style>
  </head>
<body>
    <div>
       
        <form action="/updateprd/<%= productos.id %>" method="post">
          <h3>Está editando un producto</h3>
            <input type="text" name="nombre" id="nombre" value="<%= productos.nombre %>" required  minlength="3"><br>
            <input type="number" name="codigo" id="codigo" value="<%= productos.codigo %>" required><br>
            <input type="number" name="precio" id="precio" value="<%= productos.precio %>" required><br>
            <input type="text" name="marca" id="marca" value="<%= productos.marca %>" required  minlength="4"><br>
            <input type="text" name="material" id="material" value="<%= productos.material %>" required><br>
            <input type="textarea" name="descripcion" id="descripcion" value="<%= productos.descripcion %>"  minlength="10"><br>
            <br><button type="submit" class="btn btn-primary">Actualizar Producto</button>
        </form>
    </div>
</body>
</html>
