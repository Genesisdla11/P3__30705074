<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Editar Producto</title>

    
    <style>
        body {
      font-family: sans-serif;
      background-color: #ffffff;
      margin:  200px 550px;
      padding: 30px 30px;
    }
    
    form {
      width: 500px;
      margin: 0 auto;
      background-color: #ffffff;
      padding: 20px;
      border-radius: 10px;
    }
    
    
    button {
      background-color: #181e1f;
      color: #ffffff;
      font-size: 18px;
      padding: 10px;
      border-radius: 5px;
      
    }
    
    button:hover {
      background-color: rgb(42, 179, 221) 33;
    }
    
        </style>
</head>
<body>
    <div>
        <h3>Está editando un producto</h3>
        <form action="/updateprd/<%= productos.id %>" method="post">
            <input type="text" name="nombre" id="nombre" value="<%= productos.nombre %>" required maxlength="25" minlength="3"><br>
            <input type="number" name="codigo" id="codigo" value="<%= productos.codigo %>" required><br>
            <input type="number" name="precio" id="precio" value="<%= productos.precio %>" required><br>
            <input type="text" name="marca" id="marca" value="<%= productos.marca %>" required maxlength="25" minlength="4"><br>
            <input type="text" name="material" id="material" value="<%= productos.material %>" required><br>
            <input type="textarea" name="descripcion" id="descripcion" value="<%= productos.descripcion %>" maxlength="60" minlength="10"><br>
            <br><button type="submit" class="btn btn-primary">Actualizar Producto</button>
        </form>
    </div>
</body>
</html>
