<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Agregar Imagenes</title>

    <header>
      <nav>
        <h1>Agregar Imagen</h1>
          <a href="/home">Home</a>
          <a href="/imagenes">Volver</a>
      </nav>
  </header>
    <style>

    body {
      font-family: sans-serif;
      background-color: #ffffff;
      margin: 0;
      padding: 0;
    }
    
    form {
      width: 500px;
      margin: 2.5em auto;
      background-color: #f2f2f2;
      padding: 2.5em;
      border-radius: 5px;
    }
    
    label {
      display: block;
      margin-bottom: 10px;
      font-size: 18px;
      font-weight: 600;
    }
    
    input, textarea {
      width: 96%;
      padding: 10px;
      border: 1px solid #ccc;
      margin-bottom: 10px;
    }
    
    header nav a:hover{
      background-color: black;
      color: white;
    }

    button {
      background-color: #181e1f;
      color: #ffffff;
      font-size: 18px;
      border-radius: 5px;
      display: block;
      padding: 0.5rem 1rem;
      width: 200px;
      margin: 0 auto;
      
    }
    
    header{
      background-color: #f2f2f2;
      text-align: center;
      padding: 25px 0;
    }

    nav a{
      display: inline-block;
      text-decoration: none;
      color: #696969;
      margin: 0 40px;
      padding: 5px 15px;
      border-radius: 30px;
    }
    </style>
</head>
<body>
    <div>
        <form action="/aggimg" method="post">
            <input type="text" name="url" id="url" placeholder="Ingrese la url de la imagen" required  minlength="3"><br>
            <br><select name="destacado" id="destacado">
                <option value="">--Destacado--</option>
                <option value="Si">Si</option>
                <option value="No">NO</option>
            </select><br>
            <br><select name="producto_id" id="producto_id">
                <option value="" >--Producto--</option>
                <% if (productos) { %>
                    <% for (let i = 0; i < productos.length; i++){  %>
                        <% if (productos) { %>
                            <option value="<%= productos[i].id %>" id="producto_id" name="producto_id"><%= productos[i].nombre %></option> 
                        <% } %>
                    <% } %>
                <% } else { %>
                            <option value="">No existen productos en el sistema</option>
                    <% }   %>
            </select><br>
            <br><button type="submit" class="btn btn-primary">Subir Imagen</button>
        </form>
    </div>
</body>
</html>