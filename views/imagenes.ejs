<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/stylesheets/admin.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
  
  <title>Imagenes</title>
  <style>
    table {
      border-collapse: collapse;
      width: 100%;
    }

    th, td {
      border: 1px solid #ccc;
      padding: 5px;
    }

    th {
      background-color: #eee;
    }

    .botones {
      text-align: center;
    }

    table {
      border-collapse: collapse;
      width: 100%;
    }

    th, td {
      border: 1px solid #ccc;
      padding: 5px;
    }

    th {
      background-color: #eee;
    }

    .input-group{
      width:50%; left: 40px;;
      text-align:center;
      margin:auto;
    }

    td a{
      border: 1px solid #ccc;
      padding: 5px;
    }

    tbody{
      background-color: #ffffff;
    }

    thead{
      background-color: #eee;
      text-align: center;
      padding: 15px;
    }

</style>
<body>
    <header>
        <nav>
            <h3>Imagenes</h3><br> 
            <a href="/home">Home</a>
            <a href="/categorias">Categorias</a>
            <a href="/productos">Productos</a>
            <a href="/imagenes">Imagenes</a>
        </nav>
</head><br>

<body>
  <table>
    <thead>
        <tr>
          <th>N°</th>
          <th>Imagen</th>
          <th>Destacado</th>
          <td><a href="/imgagg" class="btn">Agregar</a></td>
        </tr>
    </thead>
      <tbody>
            <tr>
                <% if (imagenes) { %>
                    <% for (let i = 0; i < imagenes.length; i++){  %>
                        <% if (imagenes) { %>
                            <tr>
                                <td><%= (i + 1) %></td>
                                <td><img src="<%= imagenes[i].url %>" width="200px" height="200px"></td>
                                <td><%= imagenes[i].destacado %></td>
                                <td> 
                                    <div class="input-group">
                                        <a href="/imgdelete/<%= imagenes[i].id%>" class="btn btn-outline-primary">Eliminar</a>
                                        <a href="/imgedit/<%= imagenes[i].id%>" class="btn btn-outline-success">Actualizar</a>
                                    </div>
                                </td>
                            </tr>    
                        <% } %>
                    <% } %>
                <% } else { %>
                        <tr>
                            <th>No hay imagenes en el sistema</th>
                        </tr>
                    <% }   %>
            </tr>
        </table>
        </tr>
      </tr>
    </tbody>
  </table>
</body>
</html>
