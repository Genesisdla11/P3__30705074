<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <link rel="stylesheet" href="/stylesheets/admin.css">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
  <title>Productos</title>
  <style>


    table {
      border-collapse: collapse;
      width: 100%;
    }

    th, td {
      border: 1px solid #ccc;
      padding: 15px;
    }

    th {
      background-color: #eee;
      text-align: center;
      padding: 15px;
    }
    
    thead {
      background-color: #eee;
      text-align: center;
      padding: 15px;
    }

    .botones {
      text-align: center;
    }

    .btn-outline-primary{
      width:90%; 
      text-align: center;
      margin:20px;
      
    }

    .btn-outline-success{
      width: 90%; 
      text-align: center;
      margin:20px;
      margin-top: 0%;
    }

  </style>
   <header>
    <nav>
        <h3>Productos</h3><br> 
        <a href="/home">Home</a>
        <a href="/categorias">Categorias</a>
        <a href="/productos">Productos</a>
        <a href="/imagenes">Imagenes</a>
    </nav>
   </header>
</head>
</head>
<body>
  <table>
    <thead>
      <tr>
        <th>N°</th>
        <th>Codigo</th>
        <th>Nombre</th>
        <th>Precio</th>
        <th>Marca</th>
        <th>Material</th>
        <th>Descripcion</th>
        <td><a href="/prdagg" class="btn">Agregar</a></td>
    </tr>
  </thead>
            
    <tbody>
        <tr>
            <% if (productos) { %>
                <% for (let i = 0; i < productos.length; i++){  %>
                    <% if (productos) { %>
                        <tr>
                            <td><%= (i + 1) %></td>
                            <td><%= productos[i].codigo %></td>
                            <td><%= productos[i].nombre %></td>
                            <td><%= productos[i].precio %>$</td>
                            <td><%= productos[i].marca %></td>
                            <td><%= productos[i].material %></td>
                            <td><%= productos[i].descripcion %></td>
                            <td> 
                                <div>
                                    <a href="/prddelete/<%= productos[i].id%>" class="btn btn-outline-primary">Eliminar</a>
                                    <a href="/prdedit/<%= productos[i].id%>" class="btn btn-outline-success">Actualizar</a>
                                </div>
                            </td>
                        </tr>    
                    <% } %>
                <% } %>
            <% } else { %>
                    <tr>
                        <th>No hay productos en el sistema</th>
                    </tr>
                <% }   %>
        </tr>
    </tbody>
  </table>
</body>
</html>
