<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Agregar Producto</title>

    <header>
      <nav>
        <h1>Agregar Producto</h1>
          <a href="/home">Home</a>
          <a href="/productos">Volver</a>
      </nav>
    </header>

    <style>
    body {
      font-family: sans-serif;
      background-color: #ffffff;
      margin: 0;
      padding: 0;
    }
    
    form {
      width: 600px;
      margin: 2.5em auto;
      background-color:#f2f2f2;
      padding: 20px;
      border-radius: 10px;
     
    }

    header nav a:hover{
    background-color: black;
    color: white;
    }

    label {
      display: block;
      margin-bottom: 10px;
      font-size: 18px;
      font-weight: 600;
    }
    
    input, textarea {
      width: 94%;
      padding: 15px;
      border: 1px solid #ccc;
      margin-bottom: 10px;
    }
    
    button {
      background-color: #181e1f;
      color: #ffffff;
      font-size: 18px;
      border-radius: 5px;
      display: block;
      padding: 0.5rem 1rem;
      width: 200px;
      margin: 0 auto;
      
    }
    
    header{
      background-color: #f2f2f2;
      text-align: center;
      padding: 25px 0;
    }

    nav a{
      display: inline-block;
      text-decoration: none;
      color: #696969;
      margin: 0 40px;
      padding: 5px 15px;
      border-radius: 30px;
    }
    
    </style>
</head>
<body>
    <div>
        <form action="/aggprd" method="post">
          <h3>Ingrese un Nuevo Producto</h3>
            <input type="text" name="nombre" id="nombre" placeholder="Ingrese el nombre del producto" required maxlength="200" minlength="3">
            <input type="number" name="codigo" id="codigo" placeholder="Ingrese el codigo del producto" required>
            <input type="number" name="precio" id="precio" placeholder="Ingrese el precio del producto" required>
            <input type="text" name="marca" id="marca" placeholder="Ingrese la marca del producto" required maxlength="25" minlength="4">
            <input type="text" name="material" id="material" placeholder="Ingrese el material del producto" required><br>
            <select name="categoria_id" id="categoria_id">
                <option value="" >--Seleccione--</option>
                <% if (categorias) { %>
                    <% for (let i = 0; i < categorias.length; i++){  %>
                        <% if (categorias) { %>
                            <option value="<%= categorias[i].id %>" id="categoria_id" name="categoria_id"><%= categorias[i].nombre %></option>
    
                        <% } %>
                    <% } %>
                <% } else { %>
                            <option value="">No existen categorias en el sistema</option>
                    <% }   %>
            </select><br>
            <br><input type="textarea" name="descripcion" id="descripcion" placeholder="Ingrese la descripcion del producto" minlength="10">
            <br><button type="submit" class="btn btn-primary">Agregar Producto</button>
        </form>
    </div>
</body>
</html>
